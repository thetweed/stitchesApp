//
//  CounterRowView.swift
//  stitchesApp
//
//  Created by Laurie on 1/28/25.
//

import SwiftUI
import CoreData

/*struct CounterRowView: View {
    let counter: Counter
    
    var body: some View {
        VStack(alignment: .leading, spacing: 4) {
            HStack {
                Text(counter.name)
                    .font(.headline)
                
                Spacer()
                
                Text(counter.counterType.capitalized)
                    .font(.caption)
                    .foregroundColor(.secondary)
                    .padding(4)
                    .background(Color(.systemGray6))
                    .cornerRadius(4)
            }
            
            if counter.targetCount > 0 {
                ProgressView(
                    value: Double(counter.currentCount),
                    total: Double(counter.targetCount)
                )
                
                Text("\(counter.currentCount)/\(counter.targetCount)")
                    .font(.caption)
                    .foregroundColor(.secondary)
            }
        }
        .padding(.vertical, 4)
    }
}
*/
